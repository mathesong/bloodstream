[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "bloodstream-docker"
dynamic = ["version"]
description = "A wrapper for generating Docker and Apptainer commands using regular bloodstream syntax"
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }
authors = [
  { name = "The bloodstream developers" }
]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Science/Research",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Topic :: Scientific/Engineering :: Medical Science Apps."
]

[project.urls]
Homepage = "https://github.com/mathesong/bloodstream"
Documentation = "https://bloodstream.readthedocs.io"
Source = "https://github.com/mathesong/bloodstream/tree/main/wrapper"
Issues = "https://github.com/mathesong/bloodstream/issues"

[project.scripts]
bloodstream-docker = "bloodstream_docker.cli:main"

[tool.setuptools]
license-files = ["LICENSE"]

[tool.setuptools.dynamic]
version = { attr = "bloodstream_docker._version.__version__" }

[tool.setuptools.packages.find]
include = ["bloodstream_docker*"]
