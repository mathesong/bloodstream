% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subsetting.R
\name{apply_config_subsets}
\alias{apply_config_subsets}
\title{Apply parsed subsetting to a table of measurements}
\usage{
apply_config_subsets(measurements, config_subset)
}
\arguments{
\item{measurements}{A tibble of measurements.}

\item{config_subset}{The result of [parse_config_subsets()].}
}
\value{
`measurements`, filtered.
}
\description{
Keeps the measurements matching the inclusion grid and drops
  those matching any exclusion.

  Note the asymmetry around absent attributes: including `ses = "test"`
  drops measurements with no session at all, whereas excluding
  `ses = "-test"` keeps them, since a measurement with no session is indeed
  not `ses-test`.
}
